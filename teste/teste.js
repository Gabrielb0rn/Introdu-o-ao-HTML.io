// Este script será executado quando a página for carregada
document.addEventListener('DOMContentLoaded', () => {
    console.log('Página carregada e DOM completamente construído');
    // Podemos adicionar mais funcionalidades aqui
});
